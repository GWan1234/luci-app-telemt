# ==============================================================================
# nFPM Build Configuration
# Defines package metadata, architecture, and dependencies for IPK/APK builds.
# ==============================================================================
name: "luci-app-telemt"
platform: "linux"
version: "3.2.1-1"
section: "net"
maintainer: "Medvedolog <Medvedolog@github.com>"
description: |
  LuCI WebUI for Telemt MTProxy (v3.2.1-1).
  Features: Native DOM Strategy, VDOM Compatibility for OpenWrt 21-25. telemt 3.2.1+ supported and needed
depends:
  - libc
  - luci-base
  - luci-compat
  - qrencode
  - ca-bundle
contents:
  - src: ./usr
    dst: /usr
scripts:
  postinstall: ./scripts/postinst
  preremove: ./scripts/prerm
  postremove: ./scripts/postrm
apk:
  arch: "noarch"
ipk:
  arch: "all"

